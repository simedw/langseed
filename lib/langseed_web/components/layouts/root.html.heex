<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="LangSeed - AI-Powered Vocabulary Learning" suffix=" · LangSeed">
      {assigns[:page_title]}
    </.live_title>
    <%!-- Meta Description --%>
    <meta
      name="description"
      content={assigns[:meta_description] || "LangSeed explains new vocabulary using only words you already know, plus emojis. AI-powered language learning for Chinese, Japanese, and Swedish. Free to try."}
    />
    <%!-- Canonical URL --%>
    <link rel="canonical" href={assigns[:canonical_url] || "https://langseed.app"} />
    <%!-- Open Graph --%>
    <meta property="og:type" content="website" />
    <meta
      property="og:title"
      content={assigns[:og_title] || assigns[:page_title] || "LangSeed - AI-Powered Vocabulary Learning"}
    />
    <meta
      property="og:description"
      content={assigns[:og_description] || assigns[:meta_description] || "LangSeed explains new vocabulary using only words you already know, plus emojis. AI-powered language learning for Chinese, Japanese, and Swedish."}
    />
    <meta property="og:url" content={assigns[:canonical_url] || "https://langseed.app"} />
    <meta property="og:site_name" content="LangSeed" />
    <meta property="og:image" content={assigns[:og_image] || "https://langseed.app/images/og-preview.png"} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <%!-- Twitter Card --%>
    <meta name="twitter:card" content="summary_large_image" />
    <meta
      name="twitter:title"
      content={assigns[:og_title] || assigns[:page_title] || "LangSeed - AI-Powered Vocabulary Learning"}
    />
    <meta
      name="twitter:description"
      content={assigns[:og_description] || assigns[:meta_description] || "LangSeed explains new vocabulary using only words you already know, plus emojis. AI-powered language learning for Chinese, Japanese, and Swedish."}
    />
    <meta name="twitter:image" content={assigns[:og_image] || "https://langseed.app/images/og-preview.png"} />
    <%!-- Favicon --%>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <%!-- Preconnect hints --%>
    <link rel="preconnect" href="https://cloud.umami.is" />
    <%!-- JSON-LD Structured Data --%>
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "LangSeed",
        "applicationCategory": "EducationalApplication",
        "operatingSystem": "Web",
        "description": "AI-powered vocabulary learning app that explains new words using only vocabulary you already know, plus emojis.",
        "url": "https://langseed.app",
        "author": {
          "@type": "Person",
          "name": "Simon Edwardsson",
          "url": "https://simedw.com"
        },
        "offers": {
          "@type": "Offer",
          "price": "0",
          "priceCurrency": "USD"
        }
      }
    </script>
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        // Theme handling
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        window.addEventListener("phx:set-theme", (e) => setTheme(e.target.dataset.phxTheme));

        // Audio autoplay handling
        const setAudioAutoplay = (enabled) => {
          localStorage.setItem("phx:audio-autoplay", enabled);
          document.documentElement.setAttribute("data-audio-autoplay", enabled);
        };
        // Default to enabled
        const audioAutoplay = localStorage.getItem("phx:audio-autoplay") ?? "true";
        setAudioAutoplay(audioAutoplay);
        window.addEventListener("storage", (e) => e.key === "phx:audio-autoplay" && setAudioAutoplay(e.newValue || "true"));
        window.addEventListener("phx:toggle-audio-autoplay", () => {
          const current = localStorage.getItem("phx:audio-autoplay") !== "false";
          setAudioAutoplay(!current ? "true" : "false");
        });
      })();
    </script>
    <script
      defer
      src="https://cloud.umami.is/script.js"
      data-website-id="605e5c86-f2c3-4e64-9c14-42b8b6b8b9bd"
    >
    </script>
  </head>
  <body>
    {@inner_content}
  </body>
</html>
